server:
  port: 9082

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wisdom_hub?serverTimezone=UTC&useSSL=false
    username: root
    password: 123456
  application:
    name: wisdomHub-courseActivity
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848