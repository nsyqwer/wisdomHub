server:
  port: 9081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wisdom_hub?serverTimezone=UTC&useSSL=false
    username: root
    password: 1234
  application:
    name: wisdomHub-student
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848